# program.py 用户交互完整流程图解

## 📋 目录
1. [系统启动](#系统启动)
2. [主界面布局](#主界面布局)
3. [交互流程](#交互流程)
4. [界面展示详解](#界面展示详解)

---

## 🚀 系统启动

### 启动序列

```
用户运行 program.py
    ↓
MainWindow.__init__()
    ↓
1. setup_styles()      # 设置现代化UI样式
2. setup_ui()          # 构建界面
3. start_meta_learning() # 启动后台元学习预训练
```

---

## 🖥️ 主界面布局

### 整体结构 (1200×800 窗口)

```
┌─────────────────────────────────────────────────────────────┐
│                    Meta-PatternLearner                      │
│         Interactive Data Mining with Meta-Learning         │
│           (渐变蓝紫色标题栏，居中显示)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────┬─────────────────────────────┐ │
│  │   左侧：数据交互区       │   右侧：可视化监控区         │ │
│  │   (QTabWidget)          │   (QTabWidget)              │ │
│  │   600px                 │   600px                     │ │
│  │                         │                             │ │
│  │  Tab1: Data Loading     │   Tab1: Model Monitor       │ │
│  │  Tab2: User Interaction │   Tab2: Pattern Viz         │ │
│  │  Tab3: Results          │                             │ │
│  │                         │                             │ │
│  └─────────────────────────┴─────────────────────────────┘ │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  状态栏: Ready | 用户偏好: Hospital(3), School(2)  [进度条] │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 完整交互流程

### 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     第一步：加载数据                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
    用户点击 "Open CSV File" 按钮 (Tab1: Data Loading)
                          ↓
    ┌─────────────────────────────────────┐
    │   文件选择对话框                     │
    │   - 支持格式：*.csv                  │
    │   - 自动检测：binary/text_float/mixed│
    └─────────────────────────────────────┘
                          ↓
    ┌─────────────────────────────────────┐
    │   格式转换（如果需要）                │
    │   text_float → binary_vector        │
    │   弹出信息对话框显示转换结果          │
    └─────────────────────────────────────┘
                          ↓
    显示数据摘要：
    - 加载的模式数量
    - 特征类型列表
    - 数据样本预览（前50个）

┌─────────────────────────────────────────────────────────────┐
│                  第二步：用户交互标注                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
    用户点击 "Start Interaction" 按钮 (Tab2: User Interaction)
                          ↓
    系统根据多样性设置选择模式数量
    num_patterns = 5 + diversity_slider.value() * 2
                          ↓
    随机抽样并按复杂度排序
    selected = random.sample(auxiliary_list, num_patterns)
    selected_sorted = sorted(selected, key=lambda x: sum(x))
                          ↓
┌───────────────────────────────────────────────────────────────┐
│              弹出对话框：PatternSelectionDialog                │
│                                                               │
│  Select Interesting Patterns                                 │
│  ─────────────────────────────────────────────────────────   │
│  Please select patterns you find interesting:                │
│                                                               │
│  ┌─────────────────────────────────────────────┐            │
│  │ [ ] Pattern 1: Hospital, School  ★★☆☆☆☆☆☆☆☆│            │
│  ├─────────────────────────────────────────────┤            │
│  │ [✓] Pattern 2: Restaurant, Cinema, ★★★☆☆☆☆☆│            │
│  │                Station                       │            │
│  ├─────────────────────────────────────────────┤            │
│  │ [ ] Pattern 3: Hospital, Library,  ★★★★☆☆☆☆│            │
│  │                Museum, Theater               │            │
│  ├─────────────────────────────────────────────┤            │
│  │ [✓] Pattern 4: University, Cinema, ★★★★★☆☆☆│            │
│  │                Hospital, School, Station     │            │
│  └─────────────────────────────────────────────┘            │
│                                                               │
│              [  OK  ]       [ Cancel ]                        │
└───────────────────────────────────────────────────────────────┘
                          ↓
    用户勾选感兴趣的模式，点击OK
                          ↓
    process_user_feedback(selected_patterns)

┌─────────────────────────────────────────────────────────────┐
│                 第三步：处理用户反馈                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
    1. 分离interesting和uninteresting模式
       interesting = [p for p, selected if selected]
       uninteresting = [p for p, selected if not selected]
                          ↓
    2. 更新用户偏好统计
       user_preferences = {
           'Hospital': 5,     # 被选中5次
           'School': 3,       # 被选中3次
           'Cinema': 2        # 被选中2次
       }
                          ↓
    3. 界面更新：
       状态栏显示: "User preferences: Hospital(5), School(3), Cinema(2)"
                          ↓
    4. 检查元学习是否完成
       ├─ 已完成 → fine_tune_and_predict()
       └─ 未完成 → "Waiting for meta-learning to complete..."

┌─────────────────────────────────────────────────────────────┐
│                  第四步：模型微调                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
    fine_tune_model(interesting, uninteresting)
                          ↓
    1. 准备训练数据
       X = interesting + uninteresting
       y = [1, 1, ..., 0, 0, ...]
       train_test_split (70%/30%)
                          ↓
    2. 微调5个epoch（传统梯度下降）
       for epoch in range(5):
           - 计算loss和accuracy
           - 更新权重
           - 更新进度条
           - 实时显示指标
                          ↓
    3. 界面实时更新
       ┌───────────────────────────────────┐
       │   右侧 Tab1: Model Monitor        │
       │   ──────────────────────────────  │
       │   Meta-Loss: 0.35 ↓               │
       │   Accuracy: 87.5% ↑               │
       │   Epoch: 3/5                      │
       │   Current Task: 14 samples        │
       │                                   │
       │   [折线图显示Loss和Accuracy变化]   │
       └───────────────────────────────────┘
                          ↓
    状态栏更新: "Fine-tuning complete! Test accuracy: 100%"

┌─────────────────────────────────────────────────────────────┐
│                  第五步：预测新模式                           │
└─────────────────────────────────────────────────────────────┘
                          ↓
    predict_new_patterns()
                          ↓
    1. 对所有模式进行预测
       X_new = auxiliary_list (所有候选模式)
       y_pred = model.predict(X_new)
                          ↓
    2. 筛选感兴趣的模式 (y_pred == 1)
       predicted = [pattern for pattern, pred if pred == 1]
                          ↓
    3. 更新可视化图谱
       ┌───────────────────────────────────┐
       │   右侧 Tab2: Pattern Viz          │
       │   ──────────────────────────────  │
       │                                   │
       │        P1 ●─────● P3             │
       │         │       │                 │
       │         │   P5 ●                  │
       │         │  ╱                      │
       │        P2●                        │
       │         │                         │
       │        P4●                        │
       │                                   │
       │   Meta-Learned Pattern Relations │
       │   (网络图，节点=模式，边=相似度) │
       └───────────────────────────────────┘
                          ↓
    4. 显示结果列表
       ┌───────────────────────────────────┐
       │   左侧 Tab3: Results              │
       │   ──────────────────────────────  │
       │   Predicted interesting patterns: │
       │                                   │
       │   Pattern 1: Hospital, School     │
       │   Pattern 2: Restaurant, Cinema   │
       │   Pattern 3: University, Library  │
       │   Pattern 5: Hospital, Station    │
       │   ...                             │
       └───────────────────────────────────┘
                          ↓
    状态栏更新: "Found 8 interesting patterns"

┌─────────────────────────────────────────────────────────────┐
│                  第六步：持续学习                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
    用户可以继续交互：
    - 再次点击 "Start Interaction"
    - 标注更多模式
    - 系统累积用户偏好
    - 模型持续微调
    
    循环：交互 → 微调 → 预测 → 交互 → ...
```

---

## 🎨 界面展示详解

### 1. 左侧面板：数据交互区

#### Tab 1: Data Loading (数据加载)

```
┌──────────────────────────────────────────┐
│  📊 Data Loading & Configuration         │
├──────────────────────────────────────────┤
│                                          │
│  [Open CSV File] 按钮                    │
│    ↓ 点击后打开文件选择对话框              │
│                                          │
│  Data Information:                       │
│  ────────────────                        │
│  Loaded: 436 patterns (binary_vector)   │
│  或                                      │
│  Loaded: 15 patterns (text_float)       │
│  Features: 6                             │
│  Feature types: Cinema, Hospital, ...   │
│                                          │
│  Data Samples: (前50个样本)              │
│  ────────────────                        │
│  Pattern 1: University, Hospital         │
│  Pattern 2: Library, Restaurants         │
│  Pattern 3: Cinema, Theater              │
│  ...                                     │
│  (可滚动列表)                             │
└──────────────────────────────────────────┘
```

**操作**：
1. 点击 "Open CSV File"
2. 选择CSV文件
3. 自动检测格式并加载
4. 显示数据摘要和样本

---

#### Tab 2: User Interaction (用户交互)

```
┌──────────────────────────────────────────┐
│  👤 User Interaction Settings            │
├──────────────────────────────────────────┤
│                                          │
│  Pattern Diversity:                      │
│  ─────────────────                       │
│  Low  [────●─────] High                  │
│  (滑块：控制展示模式的多样性)              │
│                                          │
│  [Start Interaction] 按钮                │
│    ↓ 点击后弹出模式选择对话框              │
│                                          │
│  Interaction History:                    │
│  ────────────────                        │
│  Round 1: Selected 3 patterns            │
│  Round 2: Selected 5 patterns            │
│  Round 3: Selected 2 patterns            │
│  ...                                     │
│                                          │
│  Current User Preferences:               │
│  ────────────────                        │
│  Hospital: ████████ (8 times)            │
│  School:   ██████ (6 times)              │
│  Cinema:   ████ (4 times)                │
│  (柱状图显示偏好统计)                     │
└──────────────────────────────────────────┘
```

**操作**：
1. 调整多样性滑块（可选）
2. 点击 "Start Interaction"
3. 在弹出对话框中勾选感兴趣的模式
4. 点击OK确认
5. 系统自动微调和预测

---

#### Tab 3: Results (结果展示)

```
┌──────────────────────────────────────────┐
│  📋 Prediction Results                   │
├──────────────────────────────────────────┤
│                                          │
│  Fine-tuning results:                    │
│  Accuracy: 82.5%                         │
│  Test Accuracy: 100%                     │
│  Training time: 2.3s                     │
│                                          │
│  ─────────────────────────────────────   │
│                                          │
│  Predicted interesting patterns:         │
│                                          │
│  Pattern 1: Hospital, School             │
│  Pattern 2: Restaurant, Cinema, Station  │
│  Pattern 3: University, Library, Museum  │
│  Pattern 4: Hospital, Pharmacy           │
│  Pattern 5: School, Library              │
│  Pattern 6: Cinema, Theater              │
│  Pattern 7: Restaurant, Hotel            │
│  Pattern 8: Station, Hospital            │
│                                          │
│  Total: 8 patterns                       │
│                                          │
│  (可滚动查看所有结果)                     │
└──────────────────────────────────────────┘
```

**显示内容**：
- 微调准确率
- 预测的感兴趣模式列表
- 每个模式包含的特征

---

### 2. 右侧面板：可视化监控区

#### Tab 1: Model Monitor (模型监控)

```
┌──────────────────────────────────────────┐
│  📈 Real-time Meta-Learning Monitor      │
├──────────────────────────────────────────┤
│                                          │
│  Current Metrics:                        │
│  ────────────────                        │
│  Meta-Loss:      0.3512  ↓ (green)      │
│  Meta-Accuracy:  87.5%   ↑ (blue)       │
│  Meta-Epoch:     8/10                    │
│  Current Task:   Task 8/10               │
│                                          │
│  Training Progress:                      │
│  ████████░░ 80%                          │
│                                          │
│  Performance Graph:                      │
│  ┌────────────────────────────────────┐ │
│  │ 1.0 ┤                               │ │
│  │     │    Accuracy (blue)            │ │
│  │ 0.8 ┤      ╱───────                 │ │
│  │     │    ╱                           │ │
│  │ 0.6 ┤  ╱                             │ │
│  │     │╱   Loss (red)                  │ │
│  │ 0.4 ┤─────╲___                       │ │
│  │     │         ╲___                   │ │
│  │ 0.2 ┤             ────               │ │
│  │     └──┬──┬──┬──┬──┬──              │ │
│  │        2  4  6  8  10 (epoch)        │ │
│  └────────────────────────────────────┘ │
│                                          │
│  Task Details:                           │
│  14 samples, 5 epochs, lr=0.004         │
└──────────────────────────────────────────┘
```

**功能**：
- 实时显示训练指标
- 动态更新折线图
- 颜色编码（Loss红色↓，Accuracy蓝色↑）

---

#### Tab 2: Pattern Visualization (模式可视化)

```
┌──────────────────────────────────────────┐
│  🔗 Meta-Learned Pattern Relationships   │
├──────────────────────────────────────────┤
│                                          │
│  [Refresh Visualization] 按钮            │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │                                    │ │
│  │         P1 ●─────────● P3         │ │
│  │        ╱  ╲         ╱              │ │
│  │       ╱    ╲       ╱               │ │
│  │      ╱      ╲     ╱                │ │
│  │   P5●        ●P4                   │ │
│  │      ╲      ╱                      │ │
│  │       ╲    ╱                       │ │
│  │        ●P2                         │ │
│  │                                    │ │
│  │   Meta-Similarity ──────────      │ │
│  │   [========█░░░░░░░░] 0.8         │ │
│  │                                    │ │
│  └────────────────────────────────────┘ │
│                                          │
│  Graph Info:                             │
│  - 节点大小 = 模式复杂度                  │
│  - 边粗细 = 相似度 (>0.75才显示)         │
│  - 紫色节点 = 预测模式                    │
│  - 可点击查看详情                         │
│                                          │
│  Hover to see pattern details           │
└──────────────────────────────────────────┘
```

**功能**：
- 使用NetworkX绘制模式关系图
- 节点 = 模式，大小反映复杂度
- 边 = 相似度，颜色深浅表示强弱
- 交互式：悬停显示详情

---

### 3. 状态栏

```
┌──────────────────────────────────────────────────────────────┐
│ Status: Fine-tuning complete! | User prefs: Hospital(5), ... │
│                                                  [████░░] 80% │
└──────────────────────────────────────────────────────────────┘
```

**显示内容**：
- 左侧：当前操作状态
- 中间：用户偏好摘要
- 右侧：进度条（训练/微调进度）

---

## 🔄 关键交互时刻

### 时刻1：格式转换对话框

```
┌─────────────────────────────────────────┐
│  ℹ Format Conversion                    │
├─────────────────────────────────────────┤
│                                         │
│  Converted text+float format to         │
│  binary vectors                         │
│                                         │
│  Features: 6                            │
│  Patterns generated: 12                 │
│  Feature types: Cinema, Hospital,       │
│                 Restaurants, School,    │
│                 Station, Theater        │
│                                         │
│              [  OK  ]                   │
└─────────────────────────────────────────┘
```

**触发**：加载text+float格式CSV时
**用途**：告知用户格式转换结果

---

### 时刻2：模式选择对话框

```
┌───────────────────────────────────────────────────┐
│  Select Interesting Patterns                      │
├───────────────────────────────────────────────────┤
│  Please select patterns you find interesting:     │
│                                                   │
│  ┌───────────────────────────────────────────┐   │
│  │ [ ] Pattern 1: Hospital, School           │   │
│  │     ★★☆☆☆☆☆☆☆☆ (复杂度2)                 │   │
│  ├───────────────────────────────────────────┤   │
│  │ [✓] Pattern 2: Restaurant, Cinema,        │   │
│  │                Station                     │   │
│  │     ★★★☆☆☆☆☆☆☆ (复杂度3)                 │   │
│  ├───────────────────────────────────────────┤   │
│  │ [ ] Pattern 3: University, Library,       │   │
│  │                Museum, Theater             │   │
│  │     ★★★★☆☆☆☆☆☆ (复杂度4)                 │   │
│  ├───────────────────────────────────────────┤   │
│  │ [✓] Pattern 4: Hospital, School,          │   │
│  │                Pharmacy, Station           │   │
│  │     ★★★★☆☆☆☆☆☆ (复杂度4)                 │   │
│  └───────────────────────────────────────────┘   │
│                                                   │
│           [  OK  ]       [ Cancel ]               │
└───────────────────────────────────────────────────┘
```

**触发**：点击 "Start Interaction"
**用途**：让用户标注感兴趣的模式
**特点**：
- 可滚动（500×600窗口）
- 显示复杂度（星星数量）
- 多选框方式
- 按复杂度排序

---

### 时刻3：警告对话框

```
┌─────────────────────────────────────────┐
│  ⚠ No Data                              │
├─────────────────────────────────────────┤
│                                         │
│  Please load data first                 │
│                                         │
│              [  OK  ]                   │
└─────────────────────────────────────────┘
```

**触发**：未加载数据就点击交互
**用途**：提醒用户先加载数据

---

## 📊 数据流转

```
CSV文件 
  ↓ load_csv_data()
auxiliary_list (二进制向量列表)
  ↓ start_interaction()
PatternSelectionDialog (用户标注)
  ↓ process_user_feedback()
interesting + uninteresting
  ↓ fine_tune_model()
微调后的模型
  ↓ predict_new_patterns()
预测结果 + 可视化
```

---

## 🎯 关键参数

### 交互参数

```python
# 模式数量
num_patterns = 5 + diversity_slider.value() * 2
# diversity_slider: 0-5
# 实际范围: 5-15个模式

# 微调参数
epochs = 5
learning_rate = 0.004
train_test_split = 70/30

# 可视化参数
similarity_threshold = 0.75  # 相似度阈值
node_size = sum(pattern) * 50 + 100  # 节点大小
```

---

## 🔧 自定义建议

### 调整展示模式数量

```python
# program.py 第1000行
num_patterns = min(5 + self.diversity_slider.value() * 2, len(self.auxiliary_list))

# 修改为：
num_patterns = min(10 + self.diversity_slider.value() * 3, len(self.auxiliary_list))
# 范围变为：10-25个模式
```

### 调整微调轮数

```python
# program.py 第1069行
for epoch in range(5):

# 修改为：
for epoch in range(10):  # 更多轮次
```

### 调整相似度阈值

```python
# program.py 第285行
threshold = 0.75

# 修改为：
threshold = 0.6  # 更多连接
# 或
threshold = 0.9  # 更少连接
```

---

## 💡 使用技巧

1. **首次使用**：
   - 加载数据 → 等待元学习完成 → 开始交互

2. **多样性控制**：
   - 低多样性：简单模式为主
   - 高多样性：复杂模式混合

3. **标注策略**：
   - 至少选择2-3个模式
   - 混合不同复杂度
   - 反映真实偏好

4. **持续学习**：
   - 多轮交互效果更好
   - 系统会累积用户偏好
   - 预测越来越准确

---

**总结**：`program.py` 提供了一个完整的、交互式的元学习模式推荐系统，通过现代化GUI让用户轻松标注数据、训练模型、查看结果！🎉


